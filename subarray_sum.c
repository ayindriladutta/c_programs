#include<stdio.h>
#include<stdlib.h>
int main(){
 	int i, *a , sum =0 , n , given_sum = 0, srt , end;
	scanf("%d %d", &n,&given_sum);
 	
	a= (int*)malloc(n*sizeof(int));

	for(i = 0 ; i < n ; i++)
		scanf("%d",&a[i]);
 	for(i = 0 ; i < n ; i++){
		sum = a[i];
		for (j = i+1 ; j < n ; j++){
			
		  
		
		
	
	
}
